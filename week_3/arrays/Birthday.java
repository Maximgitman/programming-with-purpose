public class Birthday {
    public static void main(String[] args) {
        // Read n and triels from the command line
        int n = Integer.parseInt(args[0]);
        int trials = Integer.parseInt(args[1]);
        
    }
}